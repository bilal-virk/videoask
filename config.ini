[auth]
login = 
password = 
title = 
button_text =
button_url = 
skip_if_message_already_sent = yes #if there is a record message sent to that specific email then it will skip if no, if yes then it will always send message
skip_if_message_exists = yes #yes, no, if yes it will skip sending video if message with same title (provided above) already exists
contacts_file = TestVideoask1.csv
speed = normal #slow, normal, fast